int[] myNum = {7, 15, 37, 75, 160, 3, 12};
int x, y;
int index;

void setup(){
  size(720, 480);
  background(255);
}

void draw(){
  index = int(random(7));
  x = int(random(width));
  y = int(random(height));
  fill(30);
  textSize(50);
  text(myNum[index], x, y);
  println(myNum[index]);
}
  
